import React from "react";
import { MainBlock } from "../../components";

export const TeachersCreate: React.FC = () => {
    return(
        <MainBlock breadcrumb={['Teachers', 'Create']}>
            <div>Create</div>
        </MainBlock>
    )
}